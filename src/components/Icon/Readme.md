### Import component

Use the following import to include the component in your file:

```jsx static
import Icon from '@ume-component/Icon';
```

### Examples

Standard use:

```js
<Icon icon='star' />
```